 spec:
   # 3 fields omitted
   template:
     # 1 field omitted
     spec:
       containers:
         - name: postgres
           # 2 fields omitted
-          image: "postgres:16.3"
+          image: "postgres:16.2"
