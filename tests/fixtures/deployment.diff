 spec:
   # 1 field omitted
-  replicas: 5
+  replicas: 3
   template:
     # 1 field omitted
     spec:
       # 1 field omitted
       containers:
         - name: web
           # 2 fields omitted
-          image: "myapp:v2.2.0"
+          image: "myapp:v2.1.0"
           resources:
             # 1 field omitted
             limits:
               # 1 field omitted
-              memory: 1Gi
+              memory: 512Mi
